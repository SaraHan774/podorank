{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "node": "18"
  },
  "steps": {
    "install": {
      "commands": ["npm ci --prefix ../.."]
    },
    "build-shared": {
      "inputs": [{ "step": "install" }],
      "commands": ["npm run build --prefix ../.. -w packages/shared"]
    },
    "build-backend": {
      "inputs": [{ "step": "build-shared" }],
      "commands": ["npm run build --prefix ../.. -w packages/backend"]
    }
  },
  "deploy": {
    "startCommand": "node dist/index.js"
  }
}