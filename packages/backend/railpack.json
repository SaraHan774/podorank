{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "node": "18"
  },
  "steps": {
    "install": {
      "commands": ["cd ../.. && npm ci"]
    },
    "build-shared": {
      "inputs": [{ "step": "install" }],
      "commands": ["cd ../.. && npm run build -w packages/shared"]
    },
    "build-backend": {
      "inputs": [{ "step": "build-shared" }],
      "commands": ["cd ../.. && npm run build -w packages/backend"]
    }
  },
  "deploy": {
    "startCommand": "node dist/index.js"
  }
}